# Agent Pigeon — v0.1 Public Release Check

- Date: 2026-09-23
- Branch: `release/v0.1-public-check` (from `poc/v0.1-rc` @ latest)
- Tests: **102/102 pass**, clean build

## 1. Share is public — VERIFIED

`agent-pigeon share flight|compare` is wired in the CLI. SVG output confirmed:
```xml
<svg xmlns="http://www.w3.org/2000/svg" width="560" height="300">
  <text ... font-weight="bold">🐦 Agent Pigeon — Flight Report</text>
  ...
</svg>
```
No clipping, no broken text, no private data. N/A rendering correct.

## 2. Public product surface

- `agent-pigeon flight` — ✅
- `agent-pigeon compare <A> <B>` — ✅
- `agent-pigeon replay` — ✅
- `agent-pigeon share` — ✅ (SVG generation from real sessions)
- `agent-pigeon --help` / `--version` — ✅
- No experimental/live commands exposed

## 3. Clean package test

- `npm pack`: 29 files / 38.6 kB
- Clean temp-dir install of packed tarball → all commands work
- No experimental runtime in package
- No secrets/personal paths

## 4. README first impression

Hero: "See how your coding agent actually worked." → `agent-pigeon flight` demo.
Compare section added below. Installation and privacy documented.

## 5. Claim audit

Only conservative language used: "recognized verification", "unverified
implementation stretches", "implementation changes". No dead-end detection,
no token savings, no model comparison claims.

## 6. Platform status

- Windows 11: **TESTED** (all development and validation)
- Linux (Debian 12, Docker): **TESTED** (offline container, full pass)
- macOS: **UNTESTED**
- WSL: node absent in available distro; UNTESTED

## 7. Final privacy audit

Full history scan: no secrets, no raw transcripts, no unrelated source.
Commit author email is the owner's own (ghpark0508@gmail.com) — normal git
metadata, not a privacy leak. SVG output contains only aggregate counts and
display-safe file paths (last segments).

## FINAL DECISION

**READY FOR PRIVATE EXTERNAL TESTERS** — pending owner making the repo public
and finding one external tester. All code, tests, and docs are complete.

**STOP. Awaiting user approval.**
