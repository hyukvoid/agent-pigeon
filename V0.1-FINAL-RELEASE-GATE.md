# Final Live/Distribution Release Gate — Agent Pigeon v0.1

- Date: 2026-09-23
- Branch: `release/v0.1-replay-only` @ `99973dd` (contains every validated commit)
- Product under gate: **`agent-pigeon replay` only** (live governor is experimental, not shipped)
- Final regression: **102/102 tests pass**, clean build, 0 TypeScript errors

## 1. Linux result — VALIDATED (real packed artifact)

Environment: Docker `node:20-bookworm` (Node **v20.20.2**, npm **10.8.2**, Debian 12),
container run with **`--network none`** (proves replay has no network dependency).

| Check | Result |
| --- | --- |
| Install packed tarball (offline) | ✓ `added 1 package in 1s`, bin executable |
| `agent-pigeon --help` / `--version` | ✓ |
| `replay` (human output) | ✓ 4 sessions found under a path **containing spaces** |
| `replay --json` | ✓ turn-aware counts: 5 attempts / 5 impl turns / 1 unverified stretch / 1 productive loop |
| Malformed history (garbage, truncated, ~300 KB line, Unicode, unknown event types) | ✓ exit 0, skipped safely |
| Read-only | ✓ **all history checksums identical** before/after |
| No unexpected files written | ✓ NONE outside `node_modules` |
| 5 MB single-session | ✓ parsed in 533 ms |

## 2. macOS result

**MACOS = UNTESTED.** No macOS environment is available. README platform claims were
narrowed accordingly ("Validated on Windows 11 and Linux (Debian 12); macOS is untested").

## 3. External tester test — NOT PERFORMED (honest)

No person other than the builder was available during this autonomous session, so the
required uncoached external-tester run **did not happen**. This is recorded as a gate
blocker for PUBLIC release (see decision). Nothing was simulated to pretend a human ran it.

## 4. First-value test (lead-engineer measurement, Windows, real corpus)

- Commands: 3 (`git clone… && npm install` counts as one step; `npx agent-pigeon replay`; done)
- Elapsed: install ~1 s (tarball) / build+install from clone ~10 s; replay 27 s on a
  1.2 GB / 405-session corpus, **533 ms** on a 5 MB single session, instant (0 sessions) on empty.
- Confusion points: none observed in self-test; the external-tester pass (not performed)
  is where real confusion would surface.

## 5. README corrections

Only the platform line (narrowed to validated platforms, macOS marked untested).
No other wording changed — no misunderstandings were discovered by a human, because
no human tested it.

## 6. Final support matrix (truthful)

| | Replay v0.1 |
| --- | --- |
| **Windows 11** | **VALIDATED** (install, help, version, replay, JSON, malformed, read-only) |
| **Linux (Debian 12, Node 20, offline Docker)** | **VALIDATED** (same full pass) |
| macOS | UNTESTED |
| Claude Code history | ✓ parsed (validated on 14 real sessions) |
| Codex history (rollout JSONL) | ✓ parsed (validated on 391 real sessions) |
| Kiro / other agents | ✗ not parsed |

## 7. Final package smoke

- `npm test` → 102/102 · `npm run build` → 0 errors · `npm pack` → 29 files, 37.5 kB.
- Clean temp-dir install of the FINAL tarball: bin works; only replay/help/version are
  public; **0 experimental live runtime files packaged**; content scan clean (no personal
  paths, emails, keys, transcripts).
- Package name `agent-pigeon`: **still available on npm** (not reserved, not published).
- Repository/bugs/homepage metadata now set to github.com/hyukvoid/agent-pigeon.

## 8. Public repository decision check (owner decision)

Existing history-audit result stands: **no secrets, no keys, no raw transcripts, no
unrelated source anywhere in history**. What becomes public if the owner makes the repo
public: all POC research reports (with correction banners), the failed live-governor
experiment and its dogfood report (documented honestly), and commit author metadata
(`ghpark0508@gmail.com` — the owner's chosen git identity; switching to the GitHub
noreply address affects only future commits). This is the owner's decision; nothing was
altered.

## 9. Remaining unknowns

1. External human tester pass (§3) — not performed.
2. macOS behavior — untested.
3. Claude Code's exact PostToolBatch delivery semantics — only official-docs-validated
   (matters for the experimental live governor, which is not in v0.1).

## FINAL DECISION

**HOLD — exact blockers:**

1. **External tester run** (§3): one person who did not build the project must install
   and run `replay` uncoached; their comprehension/first-value observations recorded.
2. **macOS smoke** (optional-to-blocker): either run the same five checks on a Mac, or
   keep the README narrowed exactly as now done.

Everything else passed. When blockers 1–2 clear, this is READY FOR PUBLIC v0.1 as-is —
no further code changes are expected.

**STOP. No publish, no public repo, no tag, no release, no announcements.**
